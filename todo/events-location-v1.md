# Events + Locations V1 pass

- Keep events and locations inside `acadie_sol_directory` for V1.
- Entries are durable identities.
- Locations are reusable physical/corridor/community context.
- Events are time-based records that point to entries and locations.
- Site repo consumes exported JSON snapshots only.
- Placeholder events are for UI testing and must not be presented as real public events.
- Visitor counters are intentionally not implemented until privacy/ethics are decided.
